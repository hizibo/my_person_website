# 多环境配置（按环境名覆盖 config.yaml 中的默认值）
dev:
  website:
    base_url: "http://175.178.98.241"

uat:
  website:
    base_url: "http://uat.example.com"

prod:
  website:
    base_url: "http://prod.example.com"
  browser:
    headless: true
